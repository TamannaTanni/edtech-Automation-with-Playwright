Project Setup Instructions
* Create a virtual environment from the project directory
* Activate the virtual environment
* Install all necessary dependencies listed in requirements.txt file
* Create a file named '.env' in project folder
* Set admin_username = [admin username] and admin_password = [admin password] variables in the '.env' file
* Now run the test.py file from terminal; using command: pytest -s --headed test.py
